import React from 'react'; // Importamos React

const ProductItem = ({ product, deleteProduct }) => {
    return (
        <li>
            {product.name} (${product.price}) {/* Mostramos el nombre y precio del producto */}
            <button onClick={() => deleteProduct(product.id)}>Eliminar</button> {/* Botón para eliminar al producto */}
        </li>
    );
};

export default ProductItem; // Exportamos el componente para usarlo en otros archivos